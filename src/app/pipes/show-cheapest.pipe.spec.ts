import { ShowCheapestPipe } from './show-cheapest.pipe';

describe('ShowCheapestPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCheapestPipe();
    expect(pipe).toBeTruthy();
  });
});
